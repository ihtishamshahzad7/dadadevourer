{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "database",
  "description": "Allow the application to use its local SQLite database",
  "windows": ["main"],
  "permissions": ["sql:allow-execute", "sql:allow-select", "sql:allow-load"]
}
