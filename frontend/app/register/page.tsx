"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
const API=process.env.NEXT_PUBLIC_API_URL||"http://localhost:8000/api/v1";
export default function Register(){const router=useRouter();const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");async function submit(e:FormEvent){e.preventDefault();setError("");try{const r=await fetch(`${API}/auth/register`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});const d=await r.json();if(!r.ok)throw new Error(d.detail||"Registration failed");localStorage.setItem("access_token",d.access_token);router.push("/")}catch(e:any){setError(e.message)}}return <main style={{maxWidth:420,margin:"100px auto",padding:24,fontFamily:"system-ui"}}><h1>Create account</h1><form onSubmit={submit} style={{display:"grid",gap:14,marginTop:28}}><input type="email" required placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} style={{padding:14}}/><input type="password" required minLength={8} placeholder="Password (8+ characters)" value={password} onChange={e=>setPassword(e.target.value)} style={{padding:14}}/>{error&&<div style={{color:"crimson"}}>{error}</div>}<button style={{padding:14}}>Create account</button></form></main>}